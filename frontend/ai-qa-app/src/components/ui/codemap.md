# frontend/ai-qa-app/src/components/ui/

## 职责

通用 UI 原子组件。遵循 shadcn/ui 风格模式（forwardRef + cn + variants），纯 Tailwind CSS。

## 组件

- `button.tsx`: Button — 支持 variant（default/outline/ghost）/ size（sm/lg）/ disabled 状态
- `input.tsx`: Input — 支持 label 关联 / disabled / placeholder
